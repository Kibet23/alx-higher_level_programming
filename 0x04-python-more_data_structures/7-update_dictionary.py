#!/usr/bin/python3
def update_dictionary(a_dictionary, key, value):
    """
    replaces or adds key/value in a dictionary
    Parameters:
    a_dictionary: where the key or value will be added to
    key: input
    value: input
    """
    a_dictionary[key] = value
    return (a_dictionary)
